export const selectedCoup = (state) => {
	return state.filters.coup
}
export const selectedOpora = (state) => {
	return state.filters.opora
}
export const selectedSvet = (state) => {
	return state.filters.svet
}
